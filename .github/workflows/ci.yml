name: CI/CD Pipeline Fake
on:
  pull_request:
    branches: [ "main" ]

jobs:
  teste-e-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Rodando Testes
        run: echo "Todos os testes passaram com sucesso!"
